import {PlayerController} from '@classes/player-controller';

describe('PlayerController', () => {
  it('should create an instance', () => {
    expect(new PlayerController()).toBeTruthy();
  });
});
