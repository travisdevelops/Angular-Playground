import { StrandContainerManager } from './strand-container-manager';

describe('StrandContainerManager', () => {
  it('should create an instance', () => {
    expect(new StrandContainerManager()).toBeTruthy();
  });
});
